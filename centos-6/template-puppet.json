{
  "builders": [
    {
      "name": "virtualbox-vagrant-puppet",
      "type": "virtualbox-ovf",
      "source_path": "output-virtualbox-base/packer-virtualbox-base.ovf",
      "ssh_username": "vagrant",
      "ssh_password": "vagrant",
      "ssh_port": 22,
      "ssh_wait_timeout": "10000s",
      "shutdown_command": "echo '/sbin/halt -h -p' > /tmp/shutdown.sh; echo 'vagrant'|sudo -S sh '/tmp/shutdown.sh'"
    },
    {
      "name": "vmware-vagrant-puppet",
      "type": "vmware-vmx",
      "source_path": "output-vmware-base/packer-vmware-base.vmx",
      "ssh_username": "vagrant",
      "ssh_password": "vagrant",
      "ssh_port": 22,
      "ssh_wait_timeout": "10000s",
      "shutdown_command": "echo '/sbin/halt -h -p' > /tmp/shutdown.sh; echo 'vagrant'|sudo -S sh '/tmp/shutdown.sh'"
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "execute_command": "echo 'vagrant'|sudo -S sh '{{.Path}}'",
      "override": {
        "virtualbox-vagrant-puppet": {
          "scripts": [
            "scripts/puppet.sh",
            "scripts/cleanup.sh"
          ]
        },
        "vmware-vagrant-puppet": {
          "scripts": [
            "scripts/puppet.sh",
            "scripts/cleanup.sh"
          ]
        }
      }
    }
  ],
  "post-processors": [
    {
      "type": "vagrant",
      "override": {
        "virtualbox": {
          "output": "boxes/centos-6-puppet-virtualbox.box"
        },
        "vmware": {
          "output": "boxes/centos-6-puppet-vmware.box"
        }
      }
    }
  ]
}
