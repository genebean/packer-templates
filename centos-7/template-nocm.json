{
  "builders": [
    {
      "name": "virtualbox-vagrant-nocm",
      "type": "virtualbox-ovf",
      "source_path": "output-virtualbox-base/packer-virtualbox-base.ovf",
      "ssh_username": "vagrant",
      "ssh_password": "vagrant",
      "ssh_port": 22,
      "ssh_wait_timeout": "10000s",
      "shutdown_command": "echo '/sbin/halt -h -p' > /tmp/shutdown.sh; echo 'vagrant'|sudo -S sh '/tmp/shutdown.sh'"
    },
    {
      "name": "vmware-vagrant-nocm",
      "type": "vmware-vmx",
      "source_path": "output-vmware-base/packer-vmware-base.vmx",
      "ssh_username": "vagrant",
      "ssh_password": "vagrant",
      "ssh_port": 22,
      "ssh_wait_timeout": "10000s",
      "shutdown_command": "echo '/sbin/halt -h -p' > /tmp/shutdown.sh; echo 'vagrant'|sudo -S sh '/tmp/shutdown.sh'"
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "execute_command": "echo 'vagrant'|sudo -S sh '{{.Path}}'",
      "override": {
        "virtualbox-vagrant-nocm": {
          "scripts": [
            "scripts/cleanup.sh"
          ]
        },
        "vmware-vagrant-nocm": {
          "scripts": [
            "scripts/cleanup.sh"
          ]
        }
      }
    }
  ],
  "post-processors": [
    {
      "type": "vagrant",
      "override": {
        "virtualbox": {
          "output": "boxes/centos-7-nocm-virtualbox.box"
        },
        "vmware": {
          "output": "boxes/centos-7-nocm-vmware.box"
        }
      }
    }
  ]
}
